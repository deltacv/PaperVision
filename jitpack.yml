jdk:
  - openjdk8
before_install:
  - chmod +x gradlew
install:
  - ./gradlew :clean :build :Shared:publishToMavenLocal :PaperVision:publishToMavenLocal :LwjglPlatform:publishToMavenLocal :EOCVSimPlugin:publishToMavenLocal  -x :test
