pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()

        maven { url "https://frcmaven.wpi.edu/artifactory/release" }
        maven { url "https://maven.photonvision.org/releases" }
    }
}

rootProject.name = 'VisionGraph'

include 'VisionGraph'
include 'Shared'

include 'VisionBenchPlugin'

include 'LwjglPlatform'
include 'LwjglPlatform:Standalone'

include 'AnnotationProcessor'